<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.snapp.khabar">
    <uses-permission android:name="android.permission.INTERNET">

    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".KhabarApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Khabar">
        <activity android:name=".feature_fetch_news.presentation.ui.comment.CommentsActivity"/>
        <activity android:name=".feature_fetch_news.presentation.ui.news_detail.NewsDetailActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".feature_fetch_news.presentation.ui.edit_account.EditAccountActivity"/>
        <activity android:name=".feature_fetch_news.presentation.ui.home.HomeActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>

        </activity>

    </application>

</manifest>